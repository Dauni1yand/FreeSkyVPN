# FreeSkyVPN

VPN на рекламе: нажал «включить» — выбрал, на сколько (15 мин / 1 ч / 2 ч),
посмотрел рекламу. Подписки нет. Головной сервер управляет заграничными
нодами (VLESS + Reality + XTLS Vision). Пользователи работают только через
Android-приложение; Telegram-бот — служебный, для админов.

- [`head/`](head/) — головной сервер: FastAPI, PostgreSQL, устойчивое
  управление нодами, выбор конфигов, учёт доступа
- [`bot/`](bot/) — служебный Telegram-бот: подтверждение обновлений Xray,
  тестовый доступ, привязка аккаунтов. Только для админов — всё остальное
  отсекается на входе
- [`economics/`](economics/) — сходится ли реклама с расходами на серверы
- [`android/`](android/) — Android-приложение: одна кнопка, тёмная тема,
  раздельное туннелирование российских сервисов
- [`provisioning/`](provisioning/) — разовый bootstrap новой ноды по SSH
- [`ARCHITECTURE.md`](ARCHITECTURE.md) — как всё устроено: плоскости, агент
  ноды, управление изменениями, канал до нод, что нужно приложению
- [`DEPLOY.md`](DEPLOY.md) — развёртывание на сервере: ключи, HTTPS, подключение ноды
- `smoke_test.py` — проверка развёрнутого стека по HTTP

Админка (ноды, пользователи, доступ, SNI-пул, обновления Xray, события,
журнал действий) — часть головы, на `/admin`.

Установка на чистый сервер — один скрипт: `sudo ./install.sh`. Он спрашивает
только то, что знает человек (токен бота, ваш Telegram id, домен), остальное
настраивает сам и проверяет результат.

Архитектура, схема данных и порядок работ — в
[`ARCHITECTURE.md`](ARCHITECTURE.md); актуальные технические решения по ходу
кодирования фиксируются в `head/README.md`.
