<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Only the window chrome before Compose takes over. Black rather than
        the platform default so there is no white flash between the launcher
        and the first frame — on an app that is dark-only, that flash is the
        most visible thing about it.
    -->
    <style name="Theme.FreeSkyVPN" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
