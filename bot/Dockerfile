FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1 PYTHONDONTWRITEBYTECODE=1

WORKDIR /app
# Code before install — see head/Dockerfile for why.
COPY bot/ /app/
RUN pip install --no-cache-dir .

CMD ["python", "-m", "botapp.main"]
